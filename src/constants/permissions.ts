// src/constants/permissions.ts
//
// SCAFFOLD. Today only one permission code is checked as a magic string
// (`Permissioncode === 'Task PDF'` in TaskScreen.tsx). Not worth abstracting
// for a single usage yet — but as more permission-gated features land, define
// the known codes here instead of repeating string literals, e.g.:
//
// export const PERMISSION_CODES = {
//   TaskPdf: 'Task PDF',
// } as const;
export {};
